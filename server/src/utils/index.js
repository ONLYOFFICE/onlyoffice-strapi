/*
 * (c) Copyright Ascensio System SIA 2025
 *
 * MIT Licensed
 */
import { isValidURL } from './url';
import { detectAgent } from './agent';
import { isFileEditable, isFileSupported, getFileType } from './file';

export {
  isValidURL,
  isFileEditable,
  isFileSupported,
  getFileType,
  detectAgent,
};
